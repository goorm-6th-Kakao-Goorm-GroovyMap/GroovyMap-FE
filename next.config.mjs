/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['9e26-1-241-95-127.ngrok-free.app'],
        loader: 'default',
        path: '/',
    },
}
//도메인에 허용 url추가하기
export default nextConfig
